<div class="container mb-5">
  <div class="row justify-content-center">
    <div class="col-xs-12 col-sm-10 col-md-8">
      <div class="bg-black rounded mt-4 p-2">
        <h1 class="display-3 text-center secondary-text">Categories Listing</h1>
      </div>
    </div>
  </div>

  <div class="flickr_pagination">
    <%= will_paginate @categories, :container => false %>
  </div>
  <%= render 'categories' %>
  <div class="row justify-content-center">
    <div class="col-xs-12 col-sm-10 col-md-8">
      <div class="text-center">
        <%= link_to 'Create New Category', new_category_path, class: "text-white btn btn-darkgreen" %>
      </div>
    </div>
  </div>
  <div class="flickr_pagination">
    <%= will_paginate @categories, :container => false %>
  </div>
</div>